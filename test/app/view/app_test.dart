import 'package:flutter_test/flutter_test.dart';

void main() {
  group('App', () {
    testWidgets('should display login page', (tester) async {
      // await tester.pumpWidget(const App());
      // expect(find.byType(LoginPage), findsOneWidget);
      expect(true, true);
    });
  });
}
