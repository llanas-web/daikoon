import 'package:flutter/material.dart';

class ChallengeLimitDetails extends StatelessWidget {
  const ChallengeLimitDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Limit');
  }
}
