name: shared
description: A Very Good Project created by Very Good CLI.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.3.1 <4.0.0"
  flutter: ^3.22.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  test: ^1.25.8
  very_good_analysis: ^6.0.0

dependencies:
  app_ui:
    path: ../app_ui
  env:
    path: ../env
  flutter:
    sdk: flutter
  logger: ^2.5.0
  powersync: ^1.11.2
